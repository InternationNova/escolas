@model Scholas.Models.materia_primas

@{
    ViewBag.Title = "Delete";
}

@{
   
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>materia_primas</legend>

    <div class="display-label">codigo_smk</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.codigo_smk)
    </div>

    <div class="display-label">descricao</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.descricao)
    </div>

    <div class="display-label">unidade</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.unidade)
    </div>

    <div class="display-label">categoria_materia_primas_id</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.categoria_materia_primas_id)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Voltar", "Index")
    </p>
}
